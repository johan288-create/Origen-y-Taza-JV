PORT=5000
JWT_SECRET=cambia-esto-en-produccion-minimo-32-caracteres
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=
DB_NAME=coffee_experience

# Opcional: orígenes del front separados por coma (CORS). Si no se define, se permite *.
# CORS_ORIGINS=https://tu-app.vercel.app,http://localhost:3000

# Semilla cafeterías JV (si se crea): jv.seed@origenyj.app / JV-Seed-2026
